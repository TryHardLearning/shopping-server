server:
  port: 9145

spring:
  datasource:
    generate-unique-name: false
    url: jdbc:h2:mem:shopping-server
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    properties:
      javax:
        persistence:
          validation:
            mode: none